import { StyleSheet, Text, View } from 'react-native'
import React from 'react'
import BottomNavigator from './BottomNavigator/BottomTab'

const Home = () => {
    return (
        <BottomNavigator />
    )
}

export default Home

const styles = StyleSheet.create({})